# Sumi 开发约定

交付目标：重建 Sumi，使 Agent 成为持续在线、可以被新消息打断或转向的协作者。产品要求以 `DESIGN.md` 为唯一事实来源，系统与界面要求分别以 `SYSTEM_DESIGN.md`、`UI_DESIGN.md` 为事实来源，代码只提供可验证的实现材料。

## 开始前

1. 每个 session、上下文压缩续跑或工作交接后，依次阅读本文件和 `DESIGN.md`。
2. 开始实现前，阅读 `SYSTEM_DESIGN.md`、`UI_DESIGN.md` 中与任务相关的部分。无需读取与任务无关的部分。
3. 检查工作区状态，保留用户和其他 Agent 的未提交修改。
4. 以当前工作区为实现基线。除非用户要求，禁止用 Git 历史覆盖当前文件。

## 文档职责

- 用户当前指令优先。
- `DESIGN.md` 定义产品要求：领域词汇、产品不变量、范围和验收底线。
- `SYSTEM_DESIGN.md` 定义系统要求；`UI_DESIGN.md` 定义界面要求。
- 每项要求只能由一个设计文件定义。其他文档只能链接该定义，不得复制一份摘要作为第二套规范。
- 行为、协议、数据模型或领域词汇改变时，先更新对应设计文件。
- 文档与代码冲突时，先确认预期行为，再修改文档或代码。禁止用兼容层保留两套行为。

## 中文写作

- 每句话都要提供一个可说明的事实、规则、原因或结果。删除后不影响意思的句子必须删除。
- 删除不影响含义的形容词、背景和从句。
- 直接写结论及依据。只有读者可能产生某个具体误解时，才写否定性说明。
- 比喻、新术语和概括必须帮助理解，并能展开为具体事实。否则改成直述。
- 发现内容没有用途时删除内容，不为它补充解释。
- 禁止用假对立、情绪词或夸大判断代替论证。
- 使用常用中文词和通行标点。保留有明确技术含义的英文词，并在首次出现时说明用途。
- 标题、列表和表格用于组织信息。禁止用一两个字加冒号充当句子。

## 执行与进度

- 对用户当前任务执行“检查现状、实现、定向测试、修复”。
- 编译错误、测试失败和可验证的不确定性需要继续处理。缺少外部权限或密钥、需要用户决定产品行为、或操作可能破坏数据时暂停并说明原因。
- 通过对应验收后才能声明任务完成。验证记录只保留复现完成结论所需的命令和结果。

## 实现边界

- 范围和技术栈见 [SYSTEM_DESIGN.md](SYSTEM_DESIGN.md)。
- 默认只维护当前产品模型和当前 schema。兼容读取、双写、deprecated 字段、旧入口和数据迁移需要用户明确要求。
- Server 和 Computer 只通过版本化协议交换命令、快照和回执。
- 领域规则、流程编排和外部适配分别归属一个模块，模块公开接口表达业务行为。
- 依赖只从外部适配器单向指向 application，再指向 domain 或 core。禁止共享状态、动态 JSON 或全局 service locator。
- 每项事实、状态转换和写入行为只有一个所有者和一个修改入口。
- 代码默认私有。只有跨边界契约、运行时入口和必要测试端口可以公开。
- 新实现从空 PostgreSQL 和空 Computer 目录建立。共享环境建立基线后只使用前向 migration。
- 代码只为已有边界抽象。命名要描述业务行为，函数保持单一职责。
- 注释用于说明不变量、并发原因或安全边界，普通代码不需要注释。代码注释、MR 标题与描述、commit message 统一使用英文。
- 日志和测试输出不得包含 Message、Attachment、Memory 或 Secret 正文。

## 验证

- 日常改动运行最小相关测试。阶段完成后运行对应设计文件要求的测试。
- 核心流程、并发和安全边界必须有测试。getter、derive 和依赖库已有保证不需要重复测试。
- 旧实现、旧入口、旧 schema 和无业务边界价值的抽象必须删除。
- 新数据库和新 Computer Home 可以从空状态建立并完成核心流程。
- Rust 提交前运行 `cargo fmt --all -- --check` 和 `cargo clippy --all-targets --all-features -- -D warnings`。

## Handoff

1. 每个实现任务单独提交，下一任务的实现不得混入当前提交。
2. 当前任务提交后仍有未完成任务时，在 `/tmp/handoff.md` 写非空交接文档，记录已完成内容、验证结果、当前提交、剩余任务和已知风险。
3. 交接文档只能存放在 `/tmp`，不得写入或提交到仓库；通过 superset 创建完新 session 创建成功后保留，下一次 handoff 生成时覆盖。
