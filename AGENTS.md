1. 开发前阅读根目录 `AGENTS.md`；改变产品语义、权限或持久事实前先更新 `DESIGN.md`，完成后同步验证结果与遗留风险。
2. Go 风格遵循 Google Go Style Guide 以及 Rob Pike 风格，命名准确、函数短小、主路径直接、错误尽早返回、数据流显式；优先使用具体类型，小接口由使用方定义；小作用域局部变量使用短而惯用的名字，作用域越大名字越明确。禁止机械缩写领域名、牺牲可读性，或用长变量名复述类型和上下文。
3. 只在真实重复或替换边界出现时抽象。禁止万能框架、预留式接口、无类型字符串协议，以及为了减少行数牺牲可读性和类型安全。
5. 完成前运行与改动匹配的 format、generate、lint、test 和 build，并报告实际证据、未覆盖风险和工作区状态。
6. 默认不做数据兼容：删除 legacy，只保留最新产品模型和唯一最新 schema；禁止迁移脚本、兼容读取、双写、deprecated 字段或旧入口。只有用户明确要求兼容既有数据时才设计迁移。