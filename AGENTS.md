# Sumi Next 工程规范

1. 开发前阅读根目录 `AGENTS.md`、`CLAUDE.md` 和相关 `DESIGN.md`；改变产品语义、权限或持久事实前先更新 `DESIGN.md`，完成后同步验证结果与遗留风险。
2. `CLAUDE.md` 必须是指向 `AGENTS.md` 的软链接，不维护第二份入口正文。
3. Go 遵循 Google Go Style Guide 与 Best Practices：命名准确、函数短小、主路径直接、错误尽早返回、数据流显式；优先使用具体类型，小接口由使用方定义。
4. 只在真实重复或替换边界出现时抽象。禁止万能框架、预留式接口、无类型字符串协议，以及为了减少行数牺牲可读性和类型安全。
5. 代码不写解释设计的注释；用命名、类型、测试和 `DESIGN.md` 表达意图。安全、事务、权限和故障恢复不变量必须有行为测试。
6. 测试验证公开行为和关键不变量，不复刻实现步骤；同类输入优先使用表驱动测试。删除测试前必须确认其失败不会提供独立诊断价值。
7. 完成前运行与改动匹配的 format、generate、lint、test 和 build，并报告实际证据、未覆盖风险和工作区状态。
