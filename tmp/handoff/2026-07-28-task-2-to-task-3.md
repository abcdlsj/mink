# Sumi GOAL 任务 2 → 任务 3 交接

## 当前状态

- 当前 Superset Project：`sumi`（`7441539a-df4b-49ef-a07a-b3693c4b82d5`）。
- 当前 workspace：`fix/claude-stateless-history`（`407c2aeb-eeca-4b22-8b0f-3292132a8cd3`），分支为 `main`。
- 任务 2 已完成并提交：`8c29b4d refactor: unify message publication transaction`。
- `GOAL.md` 中任务 2 已标记为“已完成”，下一个任务是任务 3“Browser API 类型生成”。

## 任务 2 实现结果

- 新增 `src/server/message_publish.rs`，在调用方已有事务内统一写入 Message、mention、Attachment 关联、Inbox、Thread subscription 和 outbox。
- Browser 主时间线、Browser Thread reply、Agent message send 和 Agent Task 根 Message 均复用该服务。
- `src/server` 运行时代码只有 `message_publish.rs` 保留 `INSERT INTO messages`。
- Human handler 继续负责 Session/Member/Channel/Thread 目标校验和幂等响应；Agent gateway 继续负责 active run、目标地址、freshness、Inbox handle 和 Agent JSON 响应。
- Computer flow 的结构化 mention 用例改为 Agent 在 Thread 内 mention，覆盖旧实现重复写 `message_mentions` 的问题。
- HTTP、CLI 和 PostgreSQL schema 未改变。

## 已通过验证

- `cargo test server::tests::collaboration_flow --all-features`
- `cargo test server::tests::task_flow --all-features -- --nocapture`
- `cargo test server::tests::computer_flow_enforces_security_boundaries --all-features -- --nocapture`
- `cargo test --test attachment_flow --all-features -- --nocapture`
- `cargo test --test agent_dm builtin_agent_reads_and_replies_to_dm_through_real_cli --all-features -- --nocapture`
- `cargo test --test agent_dm builtin_agent_replies_acks_and_defers_channel_mentions_through_real_cli --all-features -- --nocapture`
- `cargo test --test agent_dm builtin_agent_refreshes_changed_thread_and_replies_through_real_cli --all-features -- --nocapture`
- `cargo fmt --all -- --check`
- `cargo clippy --all-targets --all-features -- -D warnings`
- `git diff --check`

## 下一步

1. 依次阅读 `AGENTS.md`、`GLOSSARY.md`、`docs/design.md`、`GOAL.md`。
2. 任务 3 开始前阅读 `docs/design/01-foundations.md`、`docs/design/07-api-data.md`、`docs/design/09-delivery-acceptance.md`，并检查 Web 现状与未提交文件。
3. 使用 Rust `utoipa` schema 生成 OpenAPI，再生成 `web/src/api/types.ts`；保留 `web/src/api/client.ts` 的传输 helper 和领域命名函数。
4. 增加可重复生成命令与生成结果一致性检查，运行 Rust schema 测试、Web build/lint 和定向测试。
5. 不提交用户的 `web/package-lock.json`；每个 GOAL 任务继续单独提交和交接。
